# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET     /areas/ids                          controllers.Application.areas_ids
GET     /areas/ids/coordinate/:lat/:lng     controllers.Application.areas_ids_coordinate(lat: Double, lng: Double)
GET     /areas/:id                          controllers.Application.areas(id: Int)